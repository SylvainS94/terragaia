{# <form method="post" action="{{ path('app_legal_delete', {'id': legal.id}) }}" onsubmit="return confirm('Etes vous certain de vouloir supprimer définitivement ces Mentions légales ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legal.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash fa-beat mx-2"></i>Supprimer</button>
</form> #}
