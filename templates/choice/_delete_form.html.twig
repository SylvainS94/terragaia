<form method="post" action="{{ path('app_choice_delete', {'id': choice.id}) }}" onsubmit="return confirm('Etes vous certain de vouloir supprimer définitivement ce Type de RDV ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ choice.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash fa-beat mx-2"></i>Supprimer</button>
</form>
